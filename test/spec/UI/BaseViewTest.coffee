define(["UI/BaseView"], ()->
    #BaseViewTest.coffee test file    
    suite("BaseView", ()->
        suite("constructor", ()->
            test("TestCase", ()->
                #implement test
                chai.assert.fail()
            )
        )
    )


)