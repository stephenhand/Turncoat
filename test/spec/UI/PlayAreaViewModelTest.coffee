define(["isolate!UI/PlayAreaViewModel"], (PlayAreaViewModel)->
  suite("PlayAreaViewModel", ()->
    suite("initialise", ()->
      test("someTest", ()->
        chai.assert.true()
      )
    )
  )


)

